package ${BASE_PKG}.${DAO}${module_name};

import ${DEFAULT_BASE_PKG}.DAO${module_name}.IDefault${struct.className}DAO;

/**
 * 
 * @author Administrator
 *
 */
public interface I${struct.className}DAO extends IDefault${struct.className}DAO {
	
}

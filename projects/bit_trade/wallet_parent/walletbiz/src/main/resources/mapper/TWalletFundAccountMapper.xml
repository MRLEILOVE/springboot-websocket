<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jdcloud.provider.mapper.TwalletFundAccountMapper">
    <resultMap id="BaseResultMap" type="com.jdcloud.provider.pojo.TWalletFundAccount">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="currency" property="currency"/>
        <result column="total" property="total"/>
        <result column="transfer_frozen" property="transferFrozen"/>
        <result column="version" property="version"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <update id="changeMoney">
        UPDATE t_wallet_fund_account
        SET
            total = #{total},
            update_time = now(),
            version = version + 1
        WHERE id = #{id} AND version = #{version}
    </update>

    <update id="modifyTransferFrozen">
        UPDATE t_wallet_fund_account
		SET
			total = total - #{num},
			transfer_frozen = transfer_frozen + #{num},
			version = version + 1
		WHERE id = #{id} AND version = #{version}
    </update>

    <update id="decreaseFreeze">
        UPDATE t_wallet_fund_account
		SET
			transfer_frozen = transfer_frozen - #{num},
			version = version + 1
		WHERE id = #{id} AND version = #{version}
    </update>
</mapper>

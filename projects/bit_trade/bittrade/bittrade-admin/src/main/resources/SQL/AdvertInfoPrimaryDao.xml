<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bittrade.admin.dao.c2c.AdvertInfoPrimaryDAO">


    <select id="findPageList" resultType="com.bittrade.admin.model.domain.AdvertInfoPageDo">
		select
        u.login_name as loginName,u.nick_name as nickName,
        ad.message
        from {bit_trade}.t_advert_info as ad
        join {jd_uc}.t_user as u on ad.user_id = u.id
        where u.id = 1
    </select>
</mapper>
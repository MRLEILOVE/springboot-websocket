<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bittrade.admin.dao.sys.ISysLogininforDAO">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.bittrade.admin.model.domain.SysLogininfor">
        <id column="info_id" property="infoId" />
        <result column="login_name" property="loginName" />
        <result column="ipaddr" property="ipaddr" />
        <result column="login_location" property="loginLocation" />
        <result column="browser" property="browser" />
        <result column="os" property="os" />
        <result column="status" property="status" />
        <result column="msg" property="msg" />
        <result column="login_time" property="loginTime" />
    </resultMap>
    sdf

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        info_id AS infoId, login_name AS loginName, ipaddr, login_location AS loginLocation, browser, os, status, msg, login_time AS loginTime
    </sql>
    
    <insert id="insertLogininfor" parameterType="com.bittrade.admin.model.domain.SysLogininfor">
		insert into sys_logininfor (login_name, status, ipaddr, login_location, browser, os, msg, login_time)
		values (#{loginName}, #{status}, #{ipaddr}, #{loginLocation}, #{browser}, #{os}, #{msg}, sysdate())
	</insert>
	
	<select id="selectLogininforList" resultMap="BaseResultMap">
		select info_id,login_name,ipaddr,login_location,browser,os,status,msg,login_time from sys_logininfor
		<where>
			<if test="logininfor.ipaddr != null and logininfor.ipaddr != ''">
				AND ipaddr like concat('%', #{logininfor.ipaddr}, '%')
			</if>
			<if test="logininfor.status != null and logininfor.status != ''">
				AND status = #{logininfor.status}
			</if>
			<if test="logininfor.loginName != null and logininfor.loginName != ''">
				AND login_name like concat('%', #{logininfor.loginName}, '%')
			</if>
			<if test="logininfor.params.beginTime != null and logininfor.params.beginTime != ''"><!-- 开始时间检索 -->
				and date_format(login_time,'%y%m%d') &gt;= date_format(#{logininfor.params.beginTime},'%y%m%d')
			</if>
			<if test="logininfor.params.endTime != null and logininfor.params.endTime != ''"><!-- 结束时间检索 -->
				and date_format(login_time,'%y%m%d') &lt;= date_format(#{logininfor.params.endTime},'%y%m%d')
			</if>
		</where>
	</select>
	
	<delete id="deleteLogininforByIds" parameterType="String">
 		delete from sys_logininfor where info_id in
 		<foreach collection="array" item="infoId" open="(" separator="," close=")">
 			#{infoId}
        </foreach> 
 	</delete>
    
    <update id="cleanLogininfor">
        truncate table sys_logininfor
    </update>
    
</mapper>

<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jdcloud.provider.mapper.WalletFundAccountMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.jdcloud.provider.pojo.WalletFundAccount">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="currency" property="currency"/>
        <result column="sum" property="sum"/>
        <result column="total" property="total"/>
        <result column="transfer_frozen" property="transferFrozen"/>
        <result column="version" property="version"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMapVo" type="com.jdcloud.provider.pojo.vo.WalletFundAccountVo">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="currency" property="currency"/>
        <result column="sum" property="sum"/>
        <result column="total" property="total"/>
        <result column="transfer_frozen" property="transferFrozen"/>
        <result column="version" property="version"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>

        <result column="name" property="name"/>
        <result column="phone" property="phone"/>
        <result column="auth_stauts" property="authStauts"/>

    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
         ac.id,
         ac.user_id AS userId,
         ac.currency,

         ac.total,
         ac.transfer_frozen AS transferFrozen,
         ac.version,
         ac.create_time AS createTime,
         ac.update_time AS updateTime
    </sql>


    <select id="getWithdrawWalletBill" resultMap="BaseResultMapVo">
        select
        ca.name,
        ca.phone,
        ca.auth_stauts as authStauts,
        <include refid="Base_Column_List"/>
        from
        t_wallet_fund_account ac left join c_personal_card ca ON ca.user_id = ac.user_id
        where 1=1
        <if test="dto.params.beginTime != null and dto.params.beginTime != ''">
            AND DATE_FORMAT(ac.create_time, '%Y-%m-%d') &gt;= DATE_FORMAT(#{dto.params.beginTime},'%Y-%m-%d')
        </if>

        <if test="dto.params.endTime != null and dto.params.endTime != ''">
            AND DATE_FORMAT(ac.create_time, '%Y-%m-%d') &lt;= DATE_FORMAT(#{dto.params.endTime}, '%Y-%m-%d')
        </if>
        <if test="dto.name!=null and dto.name!=''">
            AND CONCAT(
            ca.name,
            ca.phone
            ) like concat('%',#{dto.name}, '%')
        </if>
            and  ac.user_id !=1 and ac.user_id !=2
    </select>

</mapper>

<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jdcloud.provider.mapper.SysUserOnlineMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.jdcloud.provider.pojo.SysUserOnline">
        <id column="sessionId" property="sessionId" />
        <result column="login_name" property="loginName" />
        <result column="dept_name" property="deptName" />
        <result column="ipaddr" property="ipaddr" />
        <result column="login_location" property="loginLocation" />
        <result column="browser" property="browser" />
        <result column="os" property="os" />
        <result column="status" property="status" />
        <result column="start_timestamp" property="startTimestamp" />
        <result column="last_access_time" property="lastAccessTime" />
        <result column="expire_time" property="expireTime" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        sessionId, login_name AS loginName, dept_name AS deptName, ipaddr, login_location AS loginLocation, browser, os, status, start_timestamp AS startTimestamp, last_access_time AS lastAccessTime, expire_time AS expireTime
    </sql>
    
    <sql id="selectOnlineVo">
       select sessionId, login_name, dept_name, ipaddr, login_location, browser, os, status, status, start_timestamp, last_access_time, expire_time 
	   from sys_user_online
    </sql>
    
    <select id="selectOnlineById" parameterType="String" resultMap="BaseResultMap">
		<include refid="selectOnlineVo"/>
		where sessionid = #{sessionid}
	</select>
	
	<insert id="saveOnline" parameterType="com.jdcloud.provider.pojo.SysUserOnline">
		replace into sys_user_online(sessionId, login_name, dept_name, ipaddr, login_location, browser, os, status, start_timestamp, last_access_time, expire_time)
        values (#{sessionId}, #{loginName}, #{deptName}, #{ipaddr}, #{loginLocation}, #{browser}, #{os}, #{status}, #{startTimestamp}, #{lastAccessTime}, #{expireTime})
	</insert>
	
	<delete id="deleteOnlineById" parameterType="String">
 		delete from sys_user_online where sessionId = #{sessionId}
 	</delete>
 	
 	<select id="selectUserOnlineList" resultMap="BaseResultMap">
		<include refid="selectOnlineVo"/>
		<where>
			<if test="userOnline.ipaddr != null and userOnline.ipaddr != ''">
				AND ipaddr like concat('%', #{userOnline.ipaddr}, '%')
			</if>
			<if test="userOnline.loginName != null and userOnline.loginName != ''">
				AND login_name like concat('%', #{userOnline.loginName}, '%')
			</if>
		</where>
	</select>
	
	<select id="selectOnlineByExpired" parameterType="String" resultMap="BaseResultMap">
		<include refid="selectOnlineVo"/> o 
		WHERE o.last_access_time <![CDATA[ <= ]]> #{lastAccessTime} ORDER BY o.last_access_time ASC
	</select>

</mapper>

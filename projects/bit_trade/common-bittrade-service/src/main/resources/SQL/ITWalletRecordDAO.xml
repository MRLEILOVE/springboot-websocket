<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.common.bittrade.dao.ITWalletRecordDAO">

	<!-- 通用查询映射结果 -->
	<resultMap id="BaseResultMap" type="com.bittrade.pojo.model.TWalletRecord">
		<id javaType="int" property="id" jdbcType="INTEGER" column="id" />
		<result javaType="long" property="userId" jdbcType="BIGINT" column="user_id" />
		<result javaType="int" property="currencyId" jdbcType="INTEGER" column="currency_id" />
		<result javaType="java.math.BigDecimal" property="beforeAmount" jdbcType="DECIMAL" column="before_amount" />
		<result javaType="java.math.BigDecimal" property="afterAmount" jdbcType="DECIMAL" column="after_amount" />
		<result javaType="java.math.BigDecimal" property="changeAmount" jdbcType="DECIMAL" column="change_amount" />
		<result javaType="byte" property="type" jdbcType="INTEGER" column="type" />
		<result javaType="long" property="entrustRecordId" jdbcType="BIGINT" column="entrust_record_id" />
		<result javaType="java.time.LocalDateTime" property="createTime" jdbcType="TIMESTAMP" column="create_time" />
	</resultMap>

	<!-- 通用查询结果列 -->
	<sql id="Base_Column_List">
		`user_id` AS userId, 
		`currency_id` AS currencyId, 
		`before_amount` AS beforeAmount, 
		`after_amount` AS afterAmount, 
		`change_amount` AS changeAmount, 
		`type` AS type, 
		`entrust_record_id` AS entrustRecordId, 
		`version` AS version, 
		`create_time` AS createTime
	</sql>

	<select id="queryBiBiAccountRecord" resultType="com.bittrade.pojo.vo.RecordVO">
		SELECT c.`name` AS currency,r.change_amount AS changeAmount,r.create_time AS time,IFNULL(e.fees,0) AS fee,
		CASE
		WHEN r.type = 1 THEN '转出至法币账户'
		WHEN r.type = 2 THEN '法币钱包转入'
		WHEN r.type = 3 THEN '买入'
		WHEN r.type = 4 THEN '卖出'
		WHEN r.type = 5 THEN '转出至资金账户'
		WHEN r.type = 6 THEN '资金账户转入'
		ELSE  ''
		END AS type
		FROM t_wallet_record r
		LEFT JOIN t_entrust_record e ON e.id = r.entrust_record_id
		INNER JOIN t_currency c ON c.id = r.currency_id
		WHERE r.user_id =  #{userId}
		AND r.create_time <![CDATA[>=]]>  #{beginTime}
		AND r.create_time <![CDATA[<=]]> #{endTime}
		<if test="currencyId != null and currencyId != ''">
			AND c.id = #{currencyId}
		</if>
		<if test="list != null and list.size() >0">
			AND r.type in
			<foreach  item="item" collection="list" separator="," open="(" close=")">
				#{item}
			</foreach>
		</if>
		ORDER BY r.id DESC
	</select>

</mapper>